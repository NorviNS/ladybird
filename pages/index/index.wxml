<!--index.wxml-->
<scroll-view class="container" scroll-y="true">
  <view wx:for="{{video_data}}" wx:key="{{ index }}" class="video_content">
    <video-content item="{{ item }}" needNavigate="true"/>
  </view>
</scroll-view>

<!--index.wxml-->
<view class="container">
  <video-content item="{{ item }}"
    indexId="{{ index }}"
    needNavigate="true"
    videoLengh="{{ video_data.length }}"
    wx:for="{{video_data}}"
    wx:key="{{ index }}"
    class="video_content"/>
</view>
